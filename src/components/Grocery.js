const Grocery=()=>{
    return(
        <div>
            <h2>This is the grocery component which is using the lazy 
                loading for the code splitting,dynamic loading,on demand loading etc.
                In whicch we can use Suspense componet which is given by the react.
            </h2>
        </div>
    )
}

export default Grocery;